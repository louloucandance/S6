arbres<-c(1,2,4,1,6,3,2,1,0,2,1,2,2,1,3,0,3,2,1,2,2,3,2,3)
arbres
sort(arbres)
table(arbres)
length(arbres)
unique(arbres)
cumsum(table(arbres))
table(arbres)/length(arbres)
cumsum(table(arbres)/length(arbres))
